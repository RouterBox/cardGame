# cardgame-lore-docs-game-name-references: Add "Wreck Tangle" game-name references to design/world.md, design/lore.md, and design/star-atlas.md — the last unclaimed piece of the 2026-07-29 naming directive

## Header

- unit: cardgame-lore-docs-game-name-references
- title: Add "Wreck Tangle" game-name references to design/world.md, design/lore.md, and design/star-atlas.md — the last unclaimed piece of the 2026-07-29 naming directive
- project: cardgame
- completed: 2026-07-30
- outcome: merged
- start_sha: 8b17fc026e2129bad499a217a5e3e7a4903dd8b0
- end_sha: 11f1e66143de48832fb307500939f95056eebcb5

## Intent

design/ideas-inbox.md's 2026-07-29 entry names the game "Wreck Tangle" and states: "The name should propagate through the rulebook title, the design-shelf site, the card set docs, and lore references to the game itself." test/design-game-name.test.js already locks in the first two targets (design/rules.md's title, site/index.html). The remaining 'lore references to the game itself' target is unclaimed: design/world.md (the setting overview), design/lore.md ("The Long Record", the eras chronicle), and design/star-atlas.md (the world atlas) never mention "Wreck Tangle" anywhere, even though each is exactly the kind of lore document the directive means. Add one sentence to each of the 3 files — near the top, after the H1 title, not replacing it — naming the game "Wreck Tangle" and preserving the setting/game distinction ideas-inbox.md itself states ("The Amaranth Expanse" stays the SETTING name; "Wreck Tangle" is the GAME). For design/world.md, the sentence belongs in or right after "The Setting" section. For design/lore.md, it belongs in the "Summary" section, naturally alongside its existing reference to "the present day the Alpha card set is set in." For design/star-atlas.md, it belongs in the opening paragraph, alongside its existing framing of what the atlas is for. Do not change any file's H1 title, do not touch design/rules.md, site/index.html, design/ideas-inbox.md, or any design/cards/*.md or design/playtest-*.md file — this unit is purely additive to 3 lore files. Regenerate site/design/world.html, site/design/lore.html, and site/design/star-atlas.html via tools/build-site.js. Add one new test file that mechanically asserts the string "Wreck Tangle" appears in all 3 markdown files and all 3 regenerated site pages, and that design/rules.md, site/index.html, and every file under design/cards/ are unchanged from their current content.

## Acceptance Criteria

- AC1 [inferred]: design/world.md contains the string "Wreck Tangle" at least once, and its H1 title ("# The Amaranth Expanse") is unchanged
- AC2 [inferred]: design/lore.md contains the string "Wreck Tangle" at least once, and its H1 title is unchanged
- AC3 [inferred]: design/star-atlas.md contains the string "Wreck Tangle" at least once, and its H1 title is unchanged
- AC4 [paraphrase] (held_out): design/rules.md, site/index.html, and every file under design/cards/ are byte-for-byte unchanged from their content before this unit
- AC5 [paraphrase]: site/design/world.html, site/design/lore.html, and site/design/star-atlas.html are regenerated via tools/build-site.js and each contains "Wreck Tangle"; a new test file mechanically asserts all of the above

## Plan

GATE: none

# Plan: cardgame-lore-docs-game-name-references

## Summary

`design/ideas-inbox.md`'s 2026-07-29 entry names the game **Wreck Tangle**
and says the name should propagate to "lore references to the game
itself." Two of the four propagation targets (`design/rules.md` title,
`site/index.html`) are already done and locked in by
`test/design-game-name.test.js`. This unit covers the remaining lore
targets: `design/world.md`, `design/lore.md`, and `design/star-atlas.md`.

Each file gets exactly one new sentence naming the game "Wreck Tangle",
placed per the unit spec's instructions, with H1 titles left untouched.
Then `tools/build-site.js` is run once to regenerate the three
corresponding site pages. Finally one new test file mechanically locks in
both the additions and the "nothing else changed" guarantee.

This is a purely additive documentation change — no code paths, schemas,
or generated logic are touched. Risk is low: fully reversible (revert 3
one-sentence edits + rebuild), no security/user-data/schema impact.

## Files to change

1. `design/world.md` — add one sentence (edit)
2. `design/lore.md` — add one sentence (edit)
3. `design/star-atlas.md` — add one sentence (edit)
4. `site/design/world.html`, `site/design/lore.html`,
   `site/design/star-atlas.html` — regenerated by running
   `tools/build-site.js` (do not hand-edit; the build script owns these)
5. `test/design-lore-docs-game-name-references.test.js` — new test file
   (create)

Do **not** touch: `design/rules.md`, `site/index.html`,
`design/ideas-inbox.md`, anything under `design/cards/`, or any
`design/playtest-*.md` file.

---

## Step 1 — `design/world.md`

Add one sentence as a new paragraph at the end of the "## The Setting"
section (after its second paragraph, before the "## A History in Brief"
heading). Do not touch the `# The Amaranth Expanse` H1 on line 1.

Use the Edit tool with:

old_string:
```
It is a galaxy built for traders, raiders, and builders in equal measure. A world can be a factory floor one season and a battlefield the next; a fleet you spent years growing can be gone in an afternoon, and a single overlooked outpost can quietly become an empire's spine. Diplomacy here is a tactic, not a virtue — alliances are made to be spent. What endures is not any one civilization's borders, but the five deep currents of power every civilization, however different, has learned to draw on to survive.

## A History in Brief
```

new_string:
```
It is a galaxy built for traders, raiders, and builders in equal measure. A world can be a factory floor one season and a battlefield the next; a fleet you spent years growing can be gone in an afternoon, and a single overlooked outpost can quietly become an empire's spine. Diplomacy here is a tactic, not a virtue — alliances are made to be spent. What endures is not any one civilization's borders, but the five deep currents of power every civilization, however different, has learned to draw on to survive.

*Wreck Tangle* is the card game played in the Amaranth Expanse — the Expanse is the setting; Wreck Tangle is the game.

## A History in Brief
```

Resulting file: H1 (`# The Amaranth Expanse`) unchanged, "## The Setting"
section now has three paragraphs, everything after "## A History in
Brief" untouched.

---

## Step 2 — `design/lore.md`

Add one sentence as a new paragraph at the end of the "## Summary"
section (after its existing paragraph, before the "## Timeline of Eras"
heading), naturally alongside the existing "present day the Alpha card
set is set in" reference. Do not touch the H1 on line 1
(`# The Long Record — A Chronicle of the Amaranth Expanse`).

Use the Edit tool with:

old_string:
```
Mireth Bloom fights no battles in that war, and inherits its wreckage
instead. The closing section, "Current Era: The Uneasy Expanse," fixes
where the galaxy stands at the point the Alpha card set is drawn from —
Alpha-set flavor text, and any future set's, should stay consistent with it
rather than inventing a new war or a new Fount.

## Timeline of Eras
```

new_string:
```
Mireth Bloom fights no battles in that war, and inherits its wreckage
instead. The closing section, "Current Era: The Uneasy Expanse," fixes
where the galaxy stands at the point the Alpha card set is drawn from —
Alpha-set flavor text, and any future set's, should stay consistent with it
rather than inventing a new war or a new Fount.

This chronicle is lore for *Wreck Tangle*, the card game the present day
described above belongs to; "the Amaranth Expanse" remains the setting's
name, not the game's.

## Timeline of Eras
```

Resulting file: H1 unchanged, "## Summary" section now has two
paragraphs, "## Timeline of Eras" and everything after it untouched.

---

## Step 3 — `design/star-atlas.md`

Add one sentence to the end of the opening paragraph (the paragraph
before "## Homeworlds" — this file's only paragraph before its first
`##` heading), continuing the same paragraph rather than starting a new
one, alongside its existing framing of what the atlas is for. Do not
touch the H1 on line 1 (`# The Star Atlas`).

Use the Edit tool with:

old_string:
```
The Amaranth Expanse is vast enough that no design document before this one
has ever had to name a single world in it — every rule, every race, and
every era of history has been written in the abstract. This atlas fixes
five names to the map: one Homeworld per civilization, plus a handful of
frontier and contested worlds that belong to none of them. Nothing here
changes a rule, a race's identity, or a page of lore; it only gives the
places those documents already imply somewhere to point to by name.

## Homeworlds
```

new_string:
```
The Amaranth Expanse is vast enough that no design document before this one
has ever had to name a single world in it — every rule, every race, and
every era of history has been written in the abstract. This atlas fixes
five names to the map: one Homeworld per civilization, plus a handful of
frontier and contested worlds that belong to none of them. Nothing here
changes a rule, a race's identity, or a page of lore; it only gives the
places those documents already imply somewhere to point to by name. This
atlas belongs to the card game *Wreck Tangle* — the Amaranth Expanse
remains the name of the setting it depicts, not the game.

## Homeworlds
```

Resulting file: H1 unchanged, opening paragraph now ends with the new
sentence, "## Homeworlds" and everything after it untouched.

---

## Step 4 — Regenerate the site

Run from the repo root (`C:\github\.agentbox-worktrees\cardGame\cardgame-lore-docs-game-name-references`,
or wherever the unit's working copy lives):

```
node tools/build-site.js
```

Expected stdout: `Built <N> pages into site/` (N is the current page
count — do not hardcode N anywhere, it's environment-dependent and not
part of any assertion).

What this does: `tools/build-site.js` walks every `design/**/*.md` file
(plus `gamePlan.md`), re-renders each to `site/<same path>.html`, and
skips rewriting any output whose bytes are unchanged (see
`writeFileAtomic` in `tools/build-site.js`, which reads-before-write and
returns early on an exact match). Because `design/rules.md` and every
file under `design/cards/` are untouched by Steps 1–3, their generated
outputs (`site/design/rules.html`, `site/index.html`,
`site/design/cards/*.html`) will be byte-identical to what's already on
disk and will not be rewritten. Only `site/design/world.html`,
`site/design/lore.html`, and `site/design/star-atlas.html` change,
because their markdown sources changed.

Verify manually (optional sanity check, not required for the test to
pass):
```
grep -c "Wreck Tangle" site/design/world.html site/design/lore.html site/design/star-atlas.html
```
Each should report `1` (or more).

---

## Step 5 — New test file

Create `test/design-lore-docs-game-name-references.test.js` with the
following complete content:

```js
'use strict';
const test = require('node:test');
const assert = require('node:assert');
const fs = require('node:fs');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

const REPO_ROOT = path.join(__dirname, '..');
const BUILD_SCRIPT = path.join(REPO_ROOT, 'tools', 'build-site.js');

const WORLD_MD = path.join(REPO_ROOT, 'design', 'world.md');
const LORE_MD = path.join(REPO_ROOT, 'design', 'lore.md');
const STAR_ATLAS_MD = path.join(REPO_ROOT, 'design', 'star-atlas.md');

const WORLD_HTML = path.join(REPO_ROOT, 'site', 'design', 'world.html');
const LORE_HTML = path.join(REPO_ROOT, 'site', 'design', 'lore.html');
const STAR_ATLAS_HTML = path.join(REPO_ROOT, 'site', 'design', 'star-atlas.html');

const RULES_MD = path.join(REPO_ROOT, 'design', 'rules.md');
const INDEX_HTML = path.join(REPO_ROOT, 'site', 'index.html');
const CARDS_DIR = path.join(REPO_ROOT, 'design', 'cards');

function listCardFiles() {
  return fs
    .readdirSync(CARDS_DIR)
    .filter((name) => name.endsWith('.md'))
    .map((name) => path.join(CARDS_DIR, name));
}

test('AC1: design/world.md names "Wreck Tangle" and keeps its H1', () => {
  const content = fs.readFileSync(WORLD_MD, 'utf8');
  assert.ok(content.includes('Wreck Tangle'), 'expected design/world.md to name the game "Wreck Tangle"');
  assert.ok(content.startsWith('# The Amaranth Expanse'), 'expected design/world.md H1 to be unchanged');
});

test('AC2: design/lore.md names "Wreck Tangle" and keeps its H1', () => {
  const content = fs.readFileSync(LORE_MD, 'utf8');
  assert.ok(content.includes('Wreck Tangle'), 'expected design/lore.md to name the game "Wreck Tangle"');
  assert.ok(
    content.startsWith('# The Long Record — A Chronicle of the Amaranth Expanse'),
    'expected design/lore.md H1 to be unchanged'
  );
});

test('AC3: design/star-atlas.md names "Wreck Tangle" and keeps its H1', () => {
  const content = fs.readFileSync(STAR_ATLAS_MD, 'utf8');
  assert.ok(content.includes('Wreck Tangle'), 'expected design/star-atlas.md to name the game "Wreck Tangle"');
  assert.ok(content.startsWith('# The Star Atlas'), 'expected design/star-atlas.md H1 to be unchanged');
});

test('AC4: design/rules.md, site/index.html, and design/cards/*.md are unchanged by this unit', () => {
  const beforeRules = fs.readFileSync(RULES_MD, 'utf8');
  const beforeIndex = fs.readFileSync(INDEX_HTML, 'utf8');
  const beforeCards = listCardFiles().map((p) => [p, fs.readFileSync(p, 'utf8')]);

  execFileSync(process.execPath, [BUILD_SCRIPT], { cwd: REPO_ROOT, stdio: 'pipe' });

  assert.strictEqual(fs.readFileSync(RULES_MD, 'utf8'), beforeRules, 'expected design/rules.md to be unchanged');
  assert.strictEqual(fs.readFileSync(INDEX_HTML, 'utf8'), beforeIndex, 'expected site/index.html to be unchanged');
  for (const [p, before] of beforeCards) {
    assert.strictEqual(
      fs.readFileSync(p, 'utf8'),
      before,
      `expected ${path.relative(REPO_ROOT, p)} to be unchanged`
    );
  }
});

test('AC5: site/design/{world,lore,star-atlas}.html are regenerated and name "Wreck Tangle"', () => {
  execFileSync(process.execPath, [BUILD_SCRIPT], { cwd: REPO_ROOT, stdio: 'pipe' });

  for (const htmlPath of [WORLD_HTML, LORE_HTML, STAR_ATLAS_HTML]) {
    const html = fs.readFileSync(htmlPath, 'utf8');
    assert.ok(html.includes('Wreck Tangle'), `expected ${path.relative(REPO_ROOT, htmlPath)} to name "Wreck Tangle"`);
  }
});
```

Notes on this test file:
- It follows the same pattern as the existing
  `test/design-game-name.test.js` (which locks in `design/rules.md` and
  `site/index.html`): it shells out to `node tools/build-site.js` via
  `execFileSync` rather than importing the build script's internals,
  since `tools/build-site.js` is a standalone CLI script (`main()` runs
  unconditionally at module load — see its last line) with no exports.
- `tools/build-site.js` itself serializes concurrent builds via a file
  lock (`lib/fs-lock.js`, `.site-build.lock`), so running it from two
  test files in the same `node --test` run (this one and
  `design-game-name.test.js`) is safe and expected — this is the same
  reason that lock exists per the comments at the top of
  `tools/build-site.js`.
- No hardcoded file lists beyond what's given in the unit spec;
  `design/cards/*.md` is enumerated dynamically via `readdirSync` so it
  stays correct if cards are added/removed later.

---

## Expected `node --test` output after all steps

Running `node --test` from the repo root should show all existing tests
still passing (in particular `test/design-world.test.js`,
`test/design-lore.test.js`, `test/design-star-atlas.test.js`,
`test/design-game-name.test.js`, and every `build-site*.test.js` /
`design-cards*.test.js` file, none of which assert exact byte-for-byte
content of `design/world.md`, `design/lore.md`, or
`design/star-atlas.md` — confirmed by inspection, they only check
structural things like section presence, heading counts, and specific
in-universe terms), plus 5 new passing tests from
`test/design-lore-docs-game-name-references.test.js`:

```
# pass <total including 5 new>
# fail 0
```

## Verification checklist for the builder

- [ ] `design/world.md` H1 (line 1) still reads exactly `# The Amaranth Expanse`
- [ ] `design/lore.md` H1 (line 1) still reads exactly `# The Long Record — A Chronicle of the Amaranth Expanse`
- [ ] `design/star-atlas.md` H1 (line 1) still reads exactly `# The Star Atlas`
- [ ] Each of the 3 files contains the literal substring `Wreck Tangle` exactly once, in the new sentence only
- [ ] `design/rules.md`, `site/index.html`, everything under `design/cards/`, `design/ideas-inbox.md`, and every `design/playtest-*.md` file are untouched (`git diff --stat` should list only the 3 lore `.md` files, the 3 regenerated `.html` files under `site/design/`, and the new test file)
- [ ] `node tools/build-site.js` runs clean and regenerates only `site/design/world.html`, `site/design/lore.html`, `site/design/star-atlas.html`
- [ ] `node --test` passes with 0 failures

## FIRE risk assessment

- **Reversibility**: trivial — 3 one-sentence markdown edits, 3 regenerated (deterministic) HTML files, 1 new test file. `git revert` or manual undo fully restores prior state.
- **Security impact**: none — no code, no input handling, no dependencies touched.
- **User data**: none — static design documents, no runtime data involved.
- **Schema changes**: none.

Overall: minimal-risk, purely additive documentation unit. No confirmation gate needed.


## Findings

# Blind Review — cardgame-lore-docs-game-name-references (cycle 1)

## AC coverage

- **AC1** (`design/world.md` contains "Wreck Tangle", H1 unchanged): Met. New sentence
  `*Wreck Tangle* is the card game played in the Amaranth Expanse — the Expanse is the
  setting; Wreck Tangle is the game.` added as a new paragraph at the end of "## The
  Setting", before "## A History in Brief". H1 (`# The Amaranth Expanse`, line 1) is
  outside the diff hunk — confirmed unchanged by reading the file directly.
- **AC2** (`design/lore.md` contains "Wreck Tangle", H1 unchanged): Met. New sentence
  added at the end of "## Summary", before "## Timeline of Eras", naturally continuing
  the existing "present day the Alpha card set is set in" reference ("the present day
  described above"). H1 (`# The Long Record — A Chronicle of the Amaranth Expanse`)
  confirmed unchanged.
- **AC3** (`design/star-atlas.md` contains "Wreck Tangle", H1 unchanged): Met. Sentence
  appended to the end of the existing opening paragraph (continuing the paragraph, not
  starting a new one), before "## Homeworlds". H1 (`# The Star Atlas`) confirmed
  unchanged.
- **AC5** (site pages regenerated via `tools/build-site.js`, contain "Wreck Tangle";
  new test file asserts all of the above): Met. `site/design/world.html`,
  `site/design/lore.html`, `site/design/star-atlas.html` each gained the corresponding
  `<em>Wreck Tangle</em>` text, consistent with the markdown source diffs and the
  renderer's inline `*text*` → `<em>` handling (`tools/build-site.js` `renderInline`).
  `test/design-lore-docs-game-name-references.test.js` (created in the RED-stage commit
  `b618d4f`, per the standing "RED-test artifact is not builder scope creep" rule —
  not gated) mechanically asserts all three markdown files contain the string and keep
  their H1, that the three regenerated HTML pages contain the string, and that
  `design/rules.md`, `site/index.html`, and every `design/cards/*.md` file are
  byte-identical before/after a rebuild.

Setting/game distinction preserved in all three added sentences, per the spec's
requirement (each sentence explicitly states "the Amaranth Expanse"/"the Expanse"
remains the setting name and "Wreck Tangle" is the game).

## Verification performed

- Read `design/world.md`, `design/lore.md`, `design/star-atlas.md` heads directly —
  H1 lines exactly match what the diff's context lines imply and what the test file's
  `startsWith` assertions expect (including the em dash in the lore.md H1).
- Confirmed `git status` is clean and `git show --stat HEAD` matches the reviewed diff
  exactly (no drift between the diff under review and the actual commit).
- Read `tools/build-site.js`'s `writeFileAtomic` — it reads-before-write and skips
  rewriting a file whose bytes are unchanged, which is the pre-existing mechanism the
  new test's AC4-equivalent check relies on to prove `design/rules.md`, `site/index.html`,
  and `design/cards/*.md`'s *outputs* aren't touched. This confirms the plan's claim and
  the test's assumption are both accurate.
- Confirmed `design/cards/` contains only `.md` files, so the test's `listCardFiles()`
  filter (`.md` only) doesn't silently skip any file the spec meant to protect.
- Diff itself contains no changes to `design/rules.md`, `site/index.html`,
  `design/ideas-inbox.md`, any `design/cards/*.md`, or any `design/playtest-*.md` file —
  matches the "purely additive to 3 lore files" constraint.
- Was unable to execute `node --test` in this review session (command required
  interactive approval that wasn't available); relied on static verification above
  instead, which is sufficient to confirm correctness given the mechanical nature of
  the assertions and the confirmed pre-existing skip-unchanged build behavior.

## Findings

None. No INTRODUCED issues found. Placement, content, and scope all match the unit
spec and plan precisely; forbidden files are untouched; the new test file's assertions
are consistent with the actual file contents and the build script's real behavior.

## Verdict

APPROVE


## Ledger

# Ledger

Append-only. Written by bolt.js, never by agents.

- [2026-07-30T21:34:34.792Z] **bolt:start** — unit=cardgame-lore-docs-game-name-references start_sha=8b17fc026e2129bad499a217a5e3e7a4903dd8b0 branch=bolt/cardgame-lore-docs-game-name-references worktree=C:\github\.agentbox-worktrees\cardGame\cardgame-lore-docs-game-name-references
- [2026-07-30T21:34:47.619Z] **baseline:done** — pre-edit test exit=0
- [2026-07-30T21:37:31.610Z] **plan:done** — plan.md written
- [2026-07-30T21:38:41.682Z] **tests:done** — RED verified on test/design-lore-docs-game-name-references.test.js (exit=1)
- [2026-07-30T21:44:17.620Z] **build:c1** — tests green, committed
- [2026-07-30T21:46:23.073Z] **review:c1** — APPROVE
- [2026-07-30T21:49:06.885Z] **review:devils-advocate** — APPROVE
- [2026-07-30T21:49:17.584Z] **receipt:done** — valid=true exit=0 diff=true
- [2026-07-30T21:49:17.952Z] **merge:done** — auto-merged bolt/cardgame-lore-docs-game-name-references into master (worktree)


## Receipt

```json
{
  "start_sha": "8b17fc026e2129bad499a217a5e3e7a4903dd8b0",
  "end_sha": "a11f698309b941c309c503ff0d1117c2041d5ae3",
  "diff_nonempty": true,
  "test_cmd": "node --test",
  "exit_code": 0,
  "criteria_covered": [
    {
      "id": "AC1",
      "held_out": false,
      "passed": true
    },
    {
      "id": "AC2",
      "held_out": false,
      "passed": true
    },
    {
      "id": "AC3",
      "held_out": false,
      "passed": true
    },
    {
      "id": "AC4",
      "held_out": true,
      "passed": true
    },
    {
      "id": "AC5",
      "held_out": false,
      "passed": true
    }
  ],
  "cycles": 1,
  "timestamp": "2026-07-30T21:49:17.583Z",
  "valid": true,
  "agent_reports": {
    "planner": "Plan written to `C:\\github\\AgentBox\\units\\cardgame-lore-docs-game-name-references\\plan.md`.\n\nSummary of the plan:\n- **3 exact one-sentence edits** (old_string/new_string given verbatim): `design/world.md` (end of \"The Setting\" section), `design/lore.md` (end of \"Summary\" section), `design/star-atlas.md` (end of opening paragraph) — each naming \"Wreck Tangle\" while preserving the H1 and the setting/game distinction.\n- **Regenerate the site** via `node tools/build-site.js`, which only rewrites `site/design/{world,lore,star-atlas}.html` since `writeFileAtomic` skips byte-identical outputs (verified `design/rules.md`/`design/cards/*.md` sources are untouched).\n- **New test file** `test/design-lore-docs-game-name-references.test.js` (full code provided) mirroring the existing `design-game-name.test.js` pattern, asserting \"Wreck Tangle\" presence + H1 preservation in the 3 md files, presence in the 3 regenerated html pages, and byte-for-byte no-change for `design/rules.md`, `site/index.html`, and `design/cards/*.md`.\n- Confirmed via inspection that none of the existing `design-world/lore/star-atlas.test.js` suites do exact-content matching that these additions would break.\n\nGATE: none — purely additive documentation change, fully reversible, no code/security/data/schema impact.",
    "test_writer": "The sandbox is blocking `node --test` execution pending your approval. Could you approve running this command so I can verify the new tests fail (RED check) before finishing?",
    "builder_c1": "All 1156 tests pass, 0 failures.\n\n**Summary:** Added one sentence naming \"Wreck Tangle\" to `design/world.md` (end of \"The Setting\" section), `design/lore.md` (end of \"Summary\" section), and `design/star-atlas.md` (end of opening paragraph), per plan.md exactly — no H1 titles touched. Regenerated the 3 corresponding site pages via `tools/build-site.js`. The new test file (`test/design-lore-docs-game-name-references.test.js`) was already in place from the RED commit and required no changes.\n\nOne wrinkle: a pre-existing test (`test/card-template-helper.test.js` AC4, from an earlier merged unit) asserts `git status --porcelain -- design` is empty — it checks live working-tree state, not a diff scoped to its own unit. My legitimate edits to `design/*.md` tripped it while uncommitted. I committed the cycle's changes (as this pipeline's normal flow does anyway at the end of every builder cycle) rather than touching the test, which resolved it since the check only cares about dirty state, not history. Flagging this since that test's assertion is fragile for any future unit touching `design/` mid-cycle — worth a look, but not mine to fix here.",
    "reviewer_c1": "APPROVE",
    "devils_advocate": "I ran the strongest attacks I could find against this approval:\n\n1. **Manually traced the markdown→HTML renderer** (`tools/build-site.js`) line-by-line against the diff — paragraph joining, `*em*` conversion, HTML-escaping, em-dash handling all reproduce the committed HTML exactly for all three files, including the tricky 3-line paragraph in `lore.md` that gets space-joined before rendering.\n\n2. **Checked sentence placement against the intent's per-file instructions** — `world.md`'s sentence sits inside \"The Setting\" section (after both existing paragraphs, before \"## A History in Brief\"); `lore.md`'s sits inside \"Summary\" (before \"## Timeline of Eras\"); `star-atlas.md`'s extends the opening paragraph. All three match spec exactly, and no H1 was touched.\n\n3. **Checked every pre-existing test that reads these three files** (`design-world.test.js`, `design-lore.test.js`, `design-star-atlas.test.js`) — all assert on heading-scoped section content via `parseSections`/`sectionText`, and none of the added sentences land inside a section those tests inspect (Cosmology H3s, Timeline numbered list, Homeworld H3s). None would break.\n\n4. **Checked the three Jaina-sync scripts and their tests** (`sync-founts-to-jaina`, `sync-lore-eras-to-jaina`, `sync-star-atlas-to-jaina`) — `parse-lore-markdown.js` explicitly skips the `Summary` section when building era records, and the fount/homeworld extraction is scoped to H3 subsections, none of which the new sentences touch. Record counts (5/6/8) are unaffected.\n\n5. **Verified the setting/game distinction** against `design/ideas-inbox.md`'s actual 2026-07-29 entry (commit `d3ae59e`) — the added sentences correctly preserve \"Amaranth Expanse = setting, Wreck Tangle = game\" as directed.\n\n6. **Verified untouched-file claims** — `design/rules.md`, `site/index.html`, `design/ideas-inbox.md`, and `design/cards/*.md` show no diff hunks, consistent with the new test's AC4 check.\n\nI could not construct a plausible failure scenario. The diff is ad"
  }
}
```

## Comments

(no comments.md)
