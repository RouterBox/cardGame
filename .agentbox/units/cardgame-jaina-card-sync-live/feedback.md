# Feedback

- [2026-07-28T23:05:00.000Z] (orchestrator, gate approval) Plan approved. One correction to carry into the build — the REAL Jaina REST contract, verified against the Jaina server source (Jaina_SaaS/jaina-app/app/api/v1 routes) and the official SDK (packages/jaina-sdk/src/resources/records.ts), so do NOT use the plan's placeholder `https://api.jaina.app/...`:
  - **Base URL:** `https://jaina.dev/api/v1` (keep the `JAINA_API_BASE_URL` env override, but this is the correct default).
  - **Auth:** `Authorization: Bearer <JAINA_API_KEY>` header.
  - **List (find existing record):** `GET {base}/projects/{projectSlug}/schemas/{schemaSlug}/records?package_slug={pkg}` — supports `filter[<fieldSlug>]=<value>` exact-match params (e.g. `filter[name]=...`) and returns a paginated envelope with a `data` array of records; each record has `id` and its fields under `data`.
  - **Create:** `POST {base}/projects/{projectSlug}/schemas/{schemaSlug}/records` with a FLAT JSON body: `{ "package_slug": "<pkg>", "<field>": <value>, ... }` (fields at top level next to package_slug, not nested).
  - **Update:** `PUT {base}/projects/{projectSlug}/schemas/{schemaSlug}/records/{recordId}` with a flat JSON body of the fields to set. (PUT, not PATCH.)
  - **Slugs for this game:** project `cardgame`, schema `card`, package `alpha` — cards already exist there, so the upsert's "update existing" path is the common case.
  - Upsert shape: list records for the schema/package once, index by the card's unique name field, then PUT for matches / POST for new ones. All of this stays behind the injectable client seam; tests keep using the fake client exactly as planned (AC1–AC4 unchanged).

- [2026-07-28T23:41:14.582Z] Root cause of the 3-red-cycle escalation: the RED tests encoded plan.md's PLACEHOLDER contract (single PUT to .../records/{slug}) while the builder correctly implemented the real contract from feedback.md - and builders cannot touch test/. Fixed in the pipeline (test-writer now receives feedback.md). For this retry: (1) test-writer MUST encode the real Jaina contract from feedback.md - list once GET {base}/projects/{proj}/schemas/card/records?package_slug=alpha with Bearer auth, build a name->id index from body.data[] (each record: id + fields under data), then PUT {records}/{uuid} for existing / POST {records} flat body {package_slug, ...fields} for new. There is NO update-by-slug endpoint. (2) A correct, reviewed reference implementation of lib/jaina-client.js survives on branch bolt/cardgame-jaina-card-sync-live (WIP commit 5d9ef1a) - reuse it.
