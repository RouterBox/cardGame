
- [2026-07-28T16:01:58.643Z] Resolved by orchestrator hand-recovery. Builder no-op again (plan carried complete Section 13 text, never applied across 3 cycles). Materialized by hand, rewrapped three phrases the AC regexes need contiguous (the tests match across [^.] spans so a mid-phrase line break with an intervening period-bearing cross-ref breaks them), and made design-combat.test.js forward-compatible (frozen 12-section list -> prefix assertion, same fix as the deck test). Merged cc7b1de, 435/435. Section 13: targets chosen at Queue-add, legal at choice, rechecked at resolve, single-illegal-target entries fizzle.
