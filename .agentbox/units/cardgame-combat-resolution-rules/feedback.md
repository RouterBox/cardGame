
- [2026-07-28T14:58:57.367Z] Resolved by orchestrator hand-recovery. Root cause: builder no-op class � the plan contained the COMPLETE Section 12 text but the builder never applied the append across 3 cycles (tests stayed red on a missing heading). Materialized the plan block by hand, fixed one line-wrap that split a phrase the AC regex needed contiguous, and made the deck-construction test forward-compatible (it froze the section count at exactly 11, which would have broken on every future rules append � same stale-count class as the render tests). Merged 70f972d, 429/429 green. Section 12: blocked damage to blockers, assignment order, lethal marking, damage cleanup, worked example.
