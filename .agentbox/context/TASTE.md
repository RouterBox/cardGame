# cardGame Taste Ledger

The `cardgame` project's own taste ledger (`projects.json` -> `C:/github/cardGame`),
split out of the repo-root `context/TASTE.md` on 2026-08-05 — see that file
(the `agentbox` project's own ledger) for the per-project scoping model.
Cite these by number in cardgame proposals. Entries and T-numbers are
unchanged from the original global ledger; only the file changed. Duplicate
T-numbers below (a pre-existing veto-CLI numbering race, documented in
`context/TASTE.md`) are preserved exactly as they were, not renumbered.

- T8 [direction, 2026-07-26]: The cardGame pilot (C:/github/cardGame, gamePlan.md) is locked to a DESIGN phase first — world building, rules, lore, Leonardo concept art, card design, Alpha card set — explicitly no game software yet. RouterBox: "I want AgentBox to build it all out." Design deliverables run through the normal pipeline as units whose acceptance criteria are mechanical checks on documents (file exists, names the 5 races, rules cover the phase sequence, every card names cost/types/rules text), not code. Art briefs precede art; Leonardo passes come after the words are approved.
- T9 [direction, 2026-07-26]: cardGame Producers originate the work. End goal: a design compelling enough that RouterBox ungates game software — until then it is a prototyping/design project only. The bar for rules structure is the MTG Comprehensive Rules (local copy: C:/github/mtg-agent-arena/MagicTheGatheringComprehensiveRulesDocument.txt, ~963KB) — study it as an example of how STRUCTURED a mature game's rules are (numbered sections, defined vocabulary, layered timing/priority, edge cases resolved on paper), do not rip off Magic's content. Compelling world/art/card design precedes any code; the software gate stays closed until RouterBox explicitly opens it.
- T16 [direction, 2026-07-28]: The software gate opened PARTWAY, with a bright line. RouterBox: "software for the game implementation is off limits but we can build tools for content generation and game authoring." So: rules engines, gameplay clients, anything that PLAYS the game — still gated. Tools that AUTHOR or PRESENT content — allowed and wanted. Two named wants: (1) a deterministic card authoring engine — layout code renders every structured element (frame, name, cost symbols, type line, rules text, stats) from card data; Leonardo/gen-AI produces ONLY the art-window image, composited into its slot, because generators can't do text/symbols or hold a style across runs; the card-anatomy skeleton spec is the engine's input contract. (2) A simple website to browse the design shelf (world, races, characters, rules, cards) from the phone — "I need to be able to see what we got without going over to my computer and picking through files and folders." T14's game-only focus continues to apply; these tools ARE game work.
- T17 [veto, 2026-07-28]: Two units already sitting in the 12-deep ready queue — cardgame-jaina-card-sync-dryrun and cardgame-sync-cards-to-jaina — both claim tools/sync-cards-to-jaina.js with incompatible acceptance criteria (dry-run print-only vs. live mocked-upsert); file a mechanical conflict note now instead of letting them collide mid-build. — vetoed: Conflict is real and well caught, but a conflict-note unit is not needed: orchestrator resolved it directly � retired cardgame-sync-cards-to-jaina (ledger resolution:retired) in favor of the already-running dryrun unit; Producer re-files the live-sync step after dryrun merges, specced to extend the tool.
- T19 [veto, 2026-07-28]: Replace the dry-run tool's 'live sync not yet implemented' stub with a real Jaina upsert path (mocked in tests), so Alpha-set cards actually land in Jaina as the content backbone's first live slice. — vetoed: Duplicate of ms4l0xre-cardgame-jaina-card-sync-live (the canonical live-sync step, correctly filed as security-risk since it writes to RouterBox's real Jaina account). Four re-filings of the same step accumulated because the security gate keeps it pending � that is the gate WORKING; do not re-file the live sync again until RouterBox decides ms4l0xre.
- T19 [veto, 2026-07-28]: Extend the dry-run Jaina card sync tool so the no-flag path exercises a real live-sync code path against an injectable mock upsert client, proving the wiring before real Jaina credentials are added. — vetoed: Duplicate of ms4l0xre-cardgame-jaina-card-sync-live (the canonical live-sync step, correctly filed as security-risk since it writes to RouterBox's real Jaina account). Four re-filings of the same step accumulated because the security gate keeps it pending � that is the gate WORKING; do not re-file the live sync again until RouterBox decides ms4l0xre.
- T20 [veto, 2026-07-28]: Extend tools/sync-cards-to-jaina.js so --live actually upserts card records to Jaina through an injectable, mockable client, replacing today's exit-1 'not yet implemented' stub. — vetoed: Duplicate of ms4l0xre-cardgame-jaina-card-sync-live (the canonical live-sync step, correctly filed as security-risk since it writes to RouterBox's real Jaina account). Four re-filings of the same step accumulated because the security gate keeps it pending � that is the gate WORKING; do not re-file the live sync again until RouterBox decides ms4l0xre.
- T24 [veto, 2026-07-30]: Write the missing art briefs for design/cards/wormhole-closure-cards.md's 5 cards, closing the wormhole-closure half of DESIGN-READINESS.md's Open Gap 1. — vetoed: duplicate: unit cardgame-art-briefs-wormhole-closure already merged (a2ee234) — proposal was filed while the unit was mid-build; check memory/ AND in-flight units before proposing
- T28 [pattern, 2026-07-31]: Frozen cross-unit verbatim snapshots of GROWING content (art-brief counts, covering lists, unresolved-question hedges) are the dominant escalation source now that the infrastructure bugs are fixed — four instances in one day (52->54, 54->59, alt 5->10, Section 8 hedge). Rule for the test-writer/reviewer: snapshot only what the unit itself owns and what is genuinely fixed; counts and lists that grow with every content unit get live-derived assertions (count the real file) or at-least floors, never pinned literals; prose that a known open-question unit will resolve must not be frozen as "unchanged".
